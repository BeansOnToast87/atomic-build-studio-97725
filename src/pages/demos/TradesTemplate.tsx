import DemoPage from "@/components/DemoPage";

export default function TradesTemplate() {
  return (
    <DemoPage
      title="Trades Template"
      subtitle="Click-to-call + WhatsApp log."
      mediaType="img"
      src="/assets/demos/trades-template.jpg"
      caption="Placeholder screenshot — replace with live demo shots."
      backHref="/#gallery"
    />
  );
}
